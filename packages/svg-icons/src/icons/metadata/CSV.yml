name: CSV
description: >-
    Represents a document with the letters "CSV" on it. Used to indicate
    CSV (Comma-Separated Values) files, data exports, imports, or any
    feature related to CSV data format.
keywords:
  - CSV
  - comma-separated values
  - data
  - file
  - export
  - import
  - spreadsheet
  - tabular data
  - data exchange
  - document
  - table
  - structured data
