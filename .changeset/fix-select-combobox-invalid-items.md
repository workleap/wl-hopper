---
"@hopper-ui/components": patch
---

Fixed an issue where Select and ComboBox dropdown items would display red text when the component was in an invalid state. The invalid styling now only applies to the trigger border, not the dropdown items.
