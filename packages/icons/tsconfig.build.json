{
    "extends": "./tsconfig.json",
    // Only the ambient declarations the library source needs. The storybook and vitest
    // globals in ../../types pull test-only packages into the declaration program.
    "include": ["src", "../../types/css-modules.d.ts", "../../types/img.d.ts"],
    "exclude": [
        "dist",
        "node_modules",
        "**/docs/**",
        "**/tests/**",
        "**/*.stories.ts",
        "**/*.stories.tsx",
        "**/*.test.ts",
        "**/*.test.tsx"
    ],
    "compilerOptions": {
        "customConditions": [],
        "rootDir": "src",
        "ignoreDeprecations": "6.0"
    }
}
