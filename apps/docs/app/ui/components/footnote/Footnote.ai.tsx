
const Footnote = () => {
    //Footnotes are not a widely accepted concept in markdown. we ignore it for now
    return undefined;
    //return <><h2>Footnotes</h2>{children}</>;
};

export default Footnote;
