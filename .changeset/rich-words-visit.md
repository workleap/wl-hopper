---
"@hopper-ui/styled-system": patch
"@hopper-ui/tokens": patch
"@hopper-ui/components": patch
---

Added a new core border-radius token `--hop-border-radius-2-5` (12px) and a new semantic shape token `--hop-shape-rounded-xm` that references it. Available in both `workleap` and `sharegate` themes.
